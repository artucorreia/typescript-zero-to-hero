// // exemplo 1
// let tarefaCompleta: boolean = true;
// let tarefaIncompleta: boolean = false;

// console.log(tarefaCompleta);
// console.log(tarefaIncompleta);

// // exemplo 2
// const exibirResultado = (maiorDeIdade: boolean) => maiorDeIdade ? console.log('Apto a dirigir.') : console.log('Inapto a dirigir');

// const verificarIdade = (age: number) => age >= 18 ? true : false;

// exibirResultado(verificarIdade(30));

const test: number = Infinity;
console.log(test);